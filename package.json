{
  "name": "example-fetch",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "lit-html": "^2.6.1"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "vite": "^4.1.3"
  }
}